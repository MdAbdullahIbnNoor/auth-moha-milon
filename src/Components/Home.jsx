

const Home = () => {
  // const authInfo = useContext(AuthContext);
  // console.log(authInfo);

  return (
    <div>
        <h2 className='text-5xl text-center my-56 mx-60 font-extrabold text-emerald-700'>This is Home Components for</h2>
    </div>
  )
}

export default Home